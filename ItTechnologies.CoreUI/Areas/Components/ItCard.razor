<div class="wrapper" style="flex-direction:@(ContentIsHorizontal ? "row" : "column")">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; } = default!;
    
    [Parameter]
    public bool ContentIsHorizontal { get; set; } = false;
}